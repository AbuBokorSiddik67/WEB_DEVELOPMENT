// 5. Combining Arrays
// Instructions:

// Create two arrays of your choice.
// Use the concat method to combine the two arrays into a new array.
// Print both the original arrays and the combined array using console.log().

let jubayer = ["Ami tomai valo basi","sotti"];
let Bou = ["Ami o tomai valo basi", "tin sotti"];
let biya = jubayer.concat(Bou);

let akkora = biya.join(" + ");
console.log(akkora);